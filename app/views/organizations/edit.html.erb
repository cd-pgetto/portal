<% content_for :title, "Editing org" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing org</h1>

  <%= render "form", organization: @organization %>

  <%= link_to "Show this org", @organization, class: "btn btn-accent" %>
  <%= link_to "Back to orgs", organizations_path, class: "btn btn-light" %>
</div>
