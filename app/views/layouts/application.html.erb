<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Portal" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Portal">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="drawer lg:drawer-open">
      <!--
      <input id="nav-sidebar-drawer-1" type="checkbox" class="drawer-toggle" />
      <label for="nav-sidebar-drawer-1" class="float-left btn drawer-button lg:hidden">
        Open drawer
      </label>

      <div class="drawer-side border-r-2 border-blue-500">
        <label for="nav-sidebar-drawer-1" aria-label="close sidebar" class="drawer-overlay"></label>
        <ul class="menu bg-base-100 min-h-full w-70 p-4">
          <div class="border-b">
            <h2 class="font-bold text-xl">Organization Manager</h2>
          </div>
      <li><%#= link_to "Organizations", admin_organizations_path, class: "btn btn-light" %></li>
      <li><%#= link_to "Identity Providers", identity_providers_path, class: "btn btn-light" %></li>
    </ul>
  </div>
  -->

  <%= render Views::Admin::SideNavBar.new %>

  <div class="drawer-content flex flex-col items-center">
    <main class="container max-w-6xl mx-auto p-4">
      <%= render Components::FlashMessages.new %>
      <%= yield %>
    </main>
  </div>
</body>
</html>
